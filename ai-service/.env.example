# Optional — if set, /analyze-text will use real OpenAI scoring.
# If left blank, it automatically falls back to a keyword-based heuristic
# so the endpoint still works for demos.
OPENAI_API_KEY=your_openai_api_key_here
